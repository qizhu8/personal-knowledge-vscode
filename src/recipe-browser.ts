import { RecipeRecord } from "./workflows/project-model";

function safeJson(value: unknown): string {
  return JSON.stringify(value).replace(/</g, "\\u003c");
}

export function recipeBrowserEditorDocument(recipe: RecipeRecord, environments: readonly { id: string; name: string; python: string }[] = []): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Recipe Workbench</title>
<style>
:root{color-scheme:dark;--bg:#131313;--panel:#202020;--input:#171717;--line:#454545;--text:#e7e7e7;--muted:#999;--accent:#e25555;--branch:#e5a84b;--good:#3fb950;--bad:#f85149;--focus:#75a7ff}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:13px/1.45 "IBM Plex Sans","Segoe UI",sans-serif}button,input,textarea,select{font:inherit}button{border:1px solid var(--line);border-radius:3px;background:#292929;color:var(--text);padding:6px 10px;cursor:pointer}button:hover{border-color:var(--accent)}button:active{transform:translateY(1px) scale(.98)}button:disabled{cursor:wait;opacity:.6}.primary{border-color:var(--accent);background:var(--accent);color:#fff;font-weight:700}.icon{display:grid;width:27px;height:27px;place-items:center;padding:0}.topbar{position:sticky;top:0;z-index:20;display:flex;align-items:center;justify-content:space-between;gap:15px;padding:11px 18px;border-bottom:1px solid var(--line);background:#131313ee}.identity{min-width:0}.eyebrow{color:var(--accent);font-size:9px;font-weight:800;text-transform:uppercase}.identity h1{overflow:hidden;margin:1px 0;font-size:18px;text-overflow:ellipsis;white-space:nowrap}.revision,.status{color:var(--muted);font-size:10px}.status.good{color:var(--good)}.status.bad{color:var(--bad)}.actions,.tools,.tabs{display:flex;align-items:center;gap:6px}main{display:grid;grid-template-columns:minmax(250px,340px) minmax(0,1fr);gap:14px;width:min(1500px,calc(100% - 24px));margin:auto;padding:14px 0 40px}.panel{min-width:0;border:1px solid var(--line);background:var(--panel)}.panel>header{padding:11px 13px;border-bottom:1px solid var(--line)}.panel h2{margin:0;font-size:13px}.panel header p{margin:2px 0 0;color:var(--muted);font-size:10px}.metadata{align-self:start}.fields{display:grid;gap:10px;padding:13px}.field{display:grid;gap:4px}.field>span,.collection-title strong{font-size:9px;font-weight:700;text-transform:uppercase}input,textarea,select{min-width:0;border:1px solid var(--line);border-radius:2px;outline:0;background:var(--input);color:var(--text);padding:6px 7px}input:focus,textarea:focus,select:focus{border-color:var(--accent)}textarea{width:100%;resize:vertical}.collection{border-top:1px solid var(--line);padding-top:9px}.collection-title,.definition-toolbar{display:flex;align-items:center;justify-content:space-between;gap:8px}.collection-rows{display:grid;gap:6px;margin-top:7px}.collection-row{display:grid;grid-template-columns:1fr 1.2fr auto auto;align-items:center;gap:5px}.collection-row input{width:100%}.collection-row label{display:flex;gap:3px;color:var(--muted);font-size:9px}.empty{color:var(--muted);font-size:10px}.definition-toolbar{padding:8px 10px;border-bottom:1px solid var(--line)}.tabs{border:1px solid var(--line)}.tabs button{border:0;border-right:1px solid var(--line);border-radius:0;background:transparent;color:var(--muted)}.tabs button.active{background:#383838;color:#fff}.graph-viewport{max-height:calc(100vh - 190px);overflow:auto}.graph-canvas{position:relative;min-width:780px;min-height:480px;background-color:#171717;background-image:radial-gradient(circle,#3b3b3b 1px,transparent 1px);background-size:18px 18px;transform-origin:top left}.graph-links{position:absolute;inset:0;z-index:1;width:100%;height:100%;pointer-events:none}.graph-links path{fill:none;stroke:#a0686c;stroke-width:1.6}.graph-links marker path{fill:var(--focus)}.boundary{position:absolute;z-index:3;display:grid;width:74px;height:34px;place-items:center;border:1px solid var(--focus);border-radius:3px;background:var(--panel);font-size:9px}.boundary.source{border-top:3px solid var(--good)}.boundary.sink{border-bottom:3px solid var(--accent)}.graph-node{position:absolute;z-index:4;width:240px;min-height:70px;border:1px solid var(--line);border-top:3px solid var(--accent);border-radius:3px;background:var(--panel);box-shadow:0 5px 14px #0008}.graph-node.branch{border-top-color:var(--branch)}.graph-node.selected{outline:2px solid var(--focus);outline-offset:2px}.graph-node>header{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:6px;align-items:center;min-height:42px;padding:6px 8px;border-bottom:1px solid #ffffff16;cursor:grab;touch-action:none}.graph-node small{display:block;overflow:hidden;color:var(--muted);font-size:7px;text-overflow:ellipsis;white-space:nowrap}.graph-node h3{overflow:hidden;margin:1px 0;font-size:12px;text-overflow:ellipsis;white-space:nowrap}.node-actions{display:flex;gap:3px}.node-summary{display:flex;gap:7px;padding:5px 8px;color:var(--muted);font-size:8px}.node-summary span+span{padding-left:7px;border-left:1px solid var(--line)}.json-editor{width:100%;min-height:calc(100vh - 190px);border:0;background:#151515;color:#ddd;padding:14px;font:11px/1.5 ui-monospace,monospace}.overlay{position:fixed;inset:0;z-index:50;display:flex;align-items:flex-start;justify-content:center;padding:42px 20px;background:#000b}.editor{display:flex;width:min(760px,calc(100vw - 40px));max-height:calc(100vh - 84px);flex-direction:column;border:1px solid #555;background:var(--panel);box-shadow:0 20px 60px #000}.editor>header,.editor>footer{display:flex;align-items:center;justify-content:space-between;padding:10px 12px;border-bottom:1px solid var(--line)}.editor>footer{border-top:1px solid var(--line);border-bottom:0}.editor h2{margin:0;color:#f06a63;font-size:14px}.editor-tabs{display:flex}.editor-tabs button{flex:1;border-radius:0}.editor-tabs button.active{background:#383838}.editor-body{overflow:auto;padding:13px}.editor-grid{display:grid;grid-template-columns:145px minmax(0,1fr);align-items:center;gap:8px}.editor-grid>label{color:var(--muted);font-size:10px}.wide{grid-column:1/-1}.dependency-row{display:grid;grid-template-columns:auto 1fr;gap:7px;padding:5px;border-bottom:1px solid var(--line)}.reference-row{display:grid;grid-template-columns:90px 1fr 110px auto;gap:6px;margin-bottom:6px}.danger{color:#ff7979}.dirty:after{content:' •';color:var(--accent)}
@media(max-width:760px){main{grid-template-columns:1fr;width:calc(100% - 16px)}.topbar{align-items:flex-start;flex-direction:column}.actions{width:100%;justify-content:flex-end}.graph-viewport{max-height:70vh}.editor-grid{grid-template-columns:1fr}.reference-row{grid-template-columns:1fr 1fr}.reference-row input{grid-column:1/-1}}
.graph-links{z-index:1}.graph-links path{stroke:#b66a70;stroke-linecap:round;stroke-linejoin:round}.graph-links path.boundary-edge{stroke:#8d6669;stroke-dasharray:3 3}.graph-arrow-head{position:absolute;z-index:3;width:12px;height:10px;background:var(--accent);clip-path:polygon(0 0,50% 100%,100% 0);pointer-events:none}.boundary{border-color:#8d6669}
</style></head><body><header class="topbar"><div class="identity"><span class="eyebrow">Automation Recipe</span><h1 id="page-title"></h1><div class="revision" id="revision"></div></div><div class="actions"><span class="status" id="status" role="status"></span><button id="validate">Validate</button><button class="primary" id="save">Save</button></div></header><main><section class="panel metadata"><header><h2>Metadata</h2><p>Retrieval context shared with Agent task matching.</p></header><div class="fields"><label class="field"><span>Name</span><input id="name"></label><label class="field"><span>Category</span><input id="category"></label><label class="field"><span>Description</span><textarea id="description" rows="3"></textarea></label><label class="field"><span>Applicable functions</span><textarea id="functions" rows="3"></textarea></label><label class="field"><span>Solution</span><textarea id="solution" rows="5"></textarea></label><div class="collection" data-collection="requiredInputs"><div class="collection-title"><strong>Required inputs</strong><button data-add="requiredInputs">Add</button></div><div class="collection-rows"></div></div><div class="collection" data-collection="expectedOutputs"><div class="collection-title"><strong>Expected outputs</strong><button data-add="expectedOutputs">Add</button></div><div class="collection-rows"></div></div></div></section><section class="panel"><header><h2>Definition</h2><p>Edit modules and dependencies directly in the executable Graph.</p></header><div class="definition-toolbar"><div class="tabs"><button class="active" data-mode="graph">Graph</button><button data-mode="json">JSON</button></div><div class="tools"><span class="status" id="definition-status"></span><button id="zoom-out" class="icon" title="Zoom out">−</button><button id="zoom-reset" title="Reset zoom">100%</button><button id="zoom-in" class="icon" title="Zoom in">+</button><button id="add-node">Add Module</button></div></div><div id="graph" class="graph-viewport"></div><textarea id="json" class="json-editor" hidden></textarea></section></main><div id="node-overlay"></div>
<script>
const initialRecipe=${safeJson(recipe)};let recipe=structuredClone(initialRecipe),dirty=false,mode='graph',zoom=1,selectedNodeId='',editorTab='intent';
const byId=id=>document.getElementById(id),esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
function metadata(){recipe.metadata||={};for(const key of ['applicableFunctions','requiredInputs','expectedOutputs'])recipe.metadata[key]||=[];recipe.metadata.solution||='';return recipe.metadata}function nodes(){return recipe.definition.spec.nodes}function setStatus(text,tone=''){byId('status').textContent=text;byId('status').className='status '+tone}function markDirty(){dirty=true;byId('page-title').classList.add('dirty');setStatus('Unsaved changes')}function bindField(id,read,write){const input=byId(id);input.value=read();input.oninput=()=>{write(input.value);markDirty()}}function renderHeader(){byId('page-title').textContent=recipe.name;byId('revision').textContent='Revision '+recipe.revision+' · '+recipe.executableDigest;document.title=recipe.name+' · Recipe Workbench'}
function renderCollection(name){const collection=metadata()[name],host=document.querySelector('[data-collection="'+name+'"] .collection-rows');host.innerHTML=collection.length?collection.map((item,index)=>'<div class="collection-row"><input data-i="'+index+'" data-k="name" value="'+esc(item.name)+'"><input data-i="'+index+'" data-k="description" value="'+esc(item.description)+'">'+(name==='requiredInputs'?'<label><input type="checkbox" data-i="'+index+'" data-k="required" '+(item.required!==false?'checked':'')+'> Required</label>':'')+'<button class="icon" data-remove="'+index+'">×</button></div>').join(''):'<span class="empty">None defined.</span>';host.querySelectorAll('[data-k]').forEach(input=>input.oninput=()=>{collection[+input.dataset.i][input.dataset.k]=input.type==='checkbox'?input.checked:input.value;markDirty()});host.querySelectorAll('[data-remove]').forEach(button=>button.onclick=()=>{collection.splice(+button.dataset.remove,1);renderCollection(name);markDirty()})}
function positions(){recipe.editorLayout||={nodePositions:{}};recipe.editorLayout.nodePositions||={};const list=nodes(),rank=new Map(list.map(node=>[node.nodeId,0]));for(let pass=0;pass<list.length;pass++)for(const node of list){const deps=(node.dependsOn||[]).filter(dep=>rank.has(dep.from));if(deps.length)rank.set(node.nodeId,Math.max(rank.get(node.nodeId),...deps.map(dep=>rank.get(dep.from)+1)))}const counts={};for(const node of list){const row=rank.get(node.nodeId),index=counts[row]||0;counts[row]=index+1;recipe.editorLayout.nodePositions[node.nodeId]||={x:120+index*264,y:80+row*118}}return recipe.editorLayout.nodePositions}
function graphTopology(){const incoming=new Set(),outgoing=new Set();for(const node of nodes())for(const dep of node.dependsOn||[]){if(dep.loop)continue;incoming.add(node.nodeId);outgoing.add(dep.from)}return{roots:nodes().filter(node=>!incoming.has(node.nodeId)),terminals:nodes().filter(node=>!outgoing.has(node.nodeId))}}
function boundaryLayout(pos){const topology=graphTopology(),average=list=>list.reduce((sum,node)=>sum+(pos[node.nodeId]?.x||0)+120,0)/Math.max(1,list.length),sourceX=Math.max(20,average(topology.roots)-37),sinkX=Math.max(20,average(topology.terminals)-37),sinkY=Math.max(166,...topology.terminals.map(node=>(pos[node.nodeId]?.y||0)+118));return{...topology,source:{x:sourceX,y:18},sink:{x:sinkX,y:sinkY}}}
function graphArrowHead(x,y){return '<i class="graph-arrow-head" style="left:'+(x-6)+'px;top:'+(y-10)+'px"></i>'}function renderEdges(canvas){const pos=positions(),layout=boundaryLayout(pos),paths=[],arrows=[];for(const target of nodes())for(const dep of target.dependsOn||[]){const a=pos[dep.from],b=pos[target.nodeId];if(!a||!b)continue;const x1=a.x+120,y1=a.y+70,x2=b.x+120,y2=b.y-8,mid=(y1+y2)/2;paths.push('<path class="module-edge" d="M '+x1+' '+y1+' C '+x1+' '+mid+', '+x2+' '+mid+', '+x2+' '+y2+'"></path>');arrows.push(graphArrowHead(x2,y2))}for(const node of layout.roots){const target=pos[node.nodeId],targetX=target.x+120,targetY=target.y-8;paths.unshift('<path class="boundary-edge input-edge" d="M '+(layout.source.x+37)+' '+(layout.source.y+34)+' C '+(layout.source.x+37)+' '+(layout.source.y+51)+', '+targetX+' '+(targetY-17)+', '+targetX+' '+targetY+'"></path>');arrows.unshift(graphArrowHead(targetX,targetY))}for(const node of layout.terminals){const source=pos[node.nodeId],sinkX=layout.sink.x+37,sinkY=layout.sink.y-8;paths.push('<path class="boundary-edge output-edge" d="M '+(source.x+120)+' '+(source.y+70)+' C '+(source.x+120)+' '+(source.y+87)+', '+sinkX+' '+(sinkY-17)+', '+sinkX+' '+sinkY+'"></path>');arrows.push(graphArrowHead(sinkX,sinkY))}canvas.querySelector('svg').innerHTML=paths.join('');canvas.querySelectorAll('.graph-arrow-head').forEach(arrow=>arrow.remove());canvas.insertAdjacentHTML('beforeend',arrows.join(''))}
function bindDrag(canvas){canvas.querySelectorAll('.graph-node>header').forEach(header=>header.onpointerdown=event=>{if(event.target.closest('button'))return;const card=header.parentElement,id=card.dataset.nodeId,start={...positions()[id]},x=event.clientX,y=event.clientY;header.setPointerCapture(event.pointerId);header.onpointermove=move=>{const pos=positions()[id];pos.x=Math.max(20,start.x+(move.clientX-x)/zoom);pos.y=Math.max(50,start.y+(move.clientY-y)/zoom);card.style.left=pos.x+'px';card.style.top=pos.y+'px';renderEdges(canvas)};header.onpointerup=()=>{header.onpointermove=null;markDirty()}})}
function renderGraph(){const host=byId('graph'),pos=positions(),layout=boundaryLayout(pos),width=Math.max(780,layout.source.x+94,layout.sink.x+94,...Object.values(pos).map(p=>p.x+280)),height=Math.max(360,layout.sink.y+52,...Object.values(pos).map(p=>p.y+118));host.innerHTML='<div class="graph-canvas" style="width:'+width+'px;height:'+height+'px;transform:scale('+zoom+')"><svg class="graph-links" viewBox="0 0 '+width+' '+height+'"></svg><div class="boundary source" style="left:'+layout.source.x+'px;top:'+layout.source.y+'px">Input</div>'+nodes().map(node=>'<article class="graph-node '+(node.control?.mode==='branch'?'branch ':'')+(selectedNodeId===node.nodeId?'selected':'')+'" data-node-id="'+esc(node.nodeId)+'" style="left:'+pos[node.nodeId].x+'px;top:'+pos[node.nodeId].y+'px"><header><div><small>'+esc(node.kind)+'</small><h3>'+esc(node.nodeId)+'</h3></div><div class="node-actions"><button class="icon" data-edit="'+esc(node.nodeId)+'" title="Edit module">✎</button><button class="icon danger" data-delete="'+esc(node.nodeId)+'" title="Delete module">×</button></div></header><div class="node-summary"><span>'+esc(node.control?.mode||'single')+'</span><span>'+((node.dependsOn||[]).length)+' dependencies</span></div></article>').join('')+'<div class="boundary sink" style="left:'+layout.sink.x+'px;top:'+layout.sink.y+'px">Output</div></div>';const canvas=host.firstElementChild;renderEdges(canvas);bindDrag(canvas);host.querySelectorAll('[data-edit]').forEach(button=>button.onclick=()=>openEditor(button.dataset.edit));host.querySelectorAll('[data-delete]').forEach(button=>button.onclick=()=>deleteNode(button.dataset.delete));host.querySelectorAll('.graph-node').forEach(card=>card.ondblclick=event=>{if(!event.target.closest('button'))openEditor(card.dataset.nodeId)});byId('definition-status').textContent=nodes().length+' Modules'}
function addNode(){let index=nodes().length+1,id='step-'+index;while(nodes().some(node=>node.nodeId===id))id='step-'+(++index);nodes().push({nodeId:id,kind:'pkm.step.noop/v1',config:{},dependsOn:[],ports:{inputs:['dependency'],outputs:['completion']},control:{mode:'single'},generalInstruction:''});recipe.definition.spec.completion.requiredNodes.push(id);markDirty();renderGraph();openEditor(id)}function deleteNode(id){if(nodes().length<=1)return setStatus('A Recipe must keep at least one module.','bad');recipe.definition.spec.nodes=nodes().filter(node=>node.nodeId!==id).map(node=>({...node,dependsOn:(node.dependsOn||[]).filter(dep=>dep.from!==id)}));recipe.definition.spec.completion.requiredNodes=(recipe.definition.spec.completion.requiredNodes||[]).filter(value=>value!==id);recipe.nodeBindings=(recipe.nodeBindings||[]).filter(entry=>entry.nodeId!==id);delete recipe.editorLayout?.nodePositions?.[id];selectedNodeId='';markDirty();renderGraph()}
function currentNode(){return nodes().find(node=>node.nodeId===selectedNodeId)}function entryFor(nodeId,create=false){recipe.nodeBindings||=[];let entry=recipe.nodeBindings.find(item=>item.nodeId===nodeId);if(!entry&&create){entry={nodeId,bindings:[]};recipe.nodeBindings.push(entry)}return entry}
function renderEditor(){const node=currentNode(),host=byId('node-overlay');if(!node){host.innerHTML='';return}const required=(recipe.definition.spec.completion.requiredNodes||[]).includes(node.nodeId),bindings=entryFor(node.nodeId)?.bindings||[];let body;if(editorTab==='intent')body='<div class="editor-grid"><label>Stable module ID</label><input value="'+esc(node.nodeId)+'" disabled><label>Adapter kind</label><input id="node-kind" value="'+esc(node.kind)+'"><label class="wide">Intent & guidance</label><textarea class="wide" id="node-instruction" rows="9">'+esc(node.generalInstruction||'')+'</textarea></div>';else if(editorTab==='parameters')body='<div class="editor-grid"><label>Execution mode</label><select id="node-mode"><option>single</option><option>repeat</option><option>branch</option></select><label>Input ports</label><input id="node-inputs" value="'+esc((node.ports?.inputs||[]).join(', '))+'"><label>Output ports</label><input id="node-outputs" value="'+esc((node.ports?.outputs||[]).join(', '))+'"><label>Completion</label><label><input id="node-required" type="checkbox" '+(required?'checked':'')+'> Required output</label><strong class="wide">Dependencies</strong>'+nodes().filter(item=>item.nodeId!==node.nodeId).map(item=>'<label class="dependency-row wide"><input type="checkbox" data-dependency="'+esc(item.nodeId)+'" '+((node.dependsOn||[]).some(dep=>dep.from===item.nodeId)?'checked':'')+'><span>'+esc(item.nodeId)+'</span></label>').join('')+'</div>';else body='<div><strong>Knowledge references</strong>'+bindings.map((binding,index)=>'<div class="reference-row"><select data-ref-kind="'+index+'"><option '+(binding.kind==='skill'?'selected':'')+'>skill</option><option '+(binding.kind==='note'?'selected':'')+'>note</option></select><input data-ref-id="'+index+'" value="'+esc(binding.knowledgeId)+'" placeholder="Category/Name"><select data-ref-usage="'+index+'"><option '+(binding.usage==='required'?'selected':'')+'>required</option><option '+(binding.usage==='recommended'?'selected':'')+'>recommended</option><option '+(binding.usage==='reference'?'selected':'')+'>reference</option></select><button class="icon" data-ref-remove="'+index+'">×</button></div>').join('')+'<button id="ref-add">Add Reference</button></div>';const tab=key=>'<button class="'+(editorTab===key?'active':'')+'" data-editor-tab="'+key+'">'+key[0].toUpperCase()+key.slice(1)+'</button>';host.innerHTML='<div class="overlay"><section class="editor" role="dialog" aria-modal="true"><header><div><small>Recipe component</small><h2>'+esc(node.nodeId)+'</h2></div><button id="editor-close" class="icon">×</button></header><div class="editor-tabs">'+tab('intent')+tab('parameters')+tab('references')+'</div><div class="editor-body">'+body+'</div><footer><button id="editor-delete" class="danger">Delete Module</button><button id="editor-done" class="primary">Done</button></footer></section></div>';host.querySelectorAll('[data-editor-tab]').forEach(button=>button.onclick=()=>{commitEditor();editorTab=button.dataset.editorTab;renderEditor()});byId('editor-close').onclick=closeEditor;byId('editor-done').onclick=closeEditor;byId('editor-delete').onclick=()=>{host.innerHTML='';deleteNode(node.nodeId)};if(byId('node-mode'))byId('node-mode').value=node.control?.mode||'single';if(byId('ref-add'))byId('ref-add').onclick=()=>{entryFor(node.nodeId,true).bindings.push({kind:'skill',knowledgeId:'',usage:'reference'});markDirty();renderEditor()};host.querySelectorAll('[data-ref-remove]').forEach(button=>button.onclick=()=>{const entry=entryFor(node.nodeId);entry.bindings.splice(+button.dataset.refRemove,1);markDirty();renderEditor()})}
function commitEditor(){const node=currentNode();if(!node)return;if(byId('node-kind'))node.kind=byId('node-kind').value.trim()||node.kind;if(byId('node-instruction'))node.generalInstruction=byId('node-instruction').value;if(byId('node-mode')){const value=byId('node-mode').value;node.control=value==='repeat'?{mode:'repeat',count:{kind:'fixed',value:2}}:value==='branch'?{mode:'branch',kind:'if',cases:node.ports?.outputs||['yes','no']}:{mode:'single'};}if(byId('node-inputs'))node.ports={inputs:byId('node-inputs').value.split(',').map(v=>v.trim()).filter(Boolean),outputs:byId('node-outputs').value.split(',').map(v=>v.trim()).filter(Boolean)};if(byId('node-required')){const set=new Set(recipe.definition.spec.completion.requiredNodes||[]);byId('node-required').checked?set.add(node.nodeId):set.delete(node.nodeId);recipe.definition.spec.completion.requiredNodes=[...set];}document.querySelectorAll('[data-dependency]').forEach(input=>{const exists=(node.dependsOn||[]).some(dep=>dep.from===input.dataset.dependency);if(input.checked&&!exists)(node.dependsOn||=[]).push({from:input.dataset.dependency,accept:['succeeded'],required:true});if(!input.checked&&exists)node.dependsOn=node.dependsOn.filter(dep=>dep.from!==input.dataset.dependency)});const entry=entryFor(node.nodeId);entry?.bindings.forEach((binding,index)=>{binding.kind=document.querySelector('[data-ref-kind="'+index+'"]')?.value||binding.kind;binding.knowledgeId=document.querySelector('[data-ref-id="'+index+'"]')?.value.trim()||binding.knowledgeId;binding.usage=document.querySelector('[data-ref-usage="'+index+'"]')?.value||binding.usage});markDirty()}function openEditor(id){selectedNodeId=id;editorTab='intent';renderGraph();renderEditor()}function closeEditor(){commitEditor();byId('node-overlay').innerHTML='';selectedNodeId='';renderGraph()}
function definitionFromEditor(){if(mode!=='json')return recipe.definition;try{return JSON.parse(byId('json').value)}catch(error){throw new Error('Definition JSON is invalid: '+error.message)}}function setMode(next){if(next==='graph'&&mode==='json')try{recipe.definition=definitionFromEditor()}catch(error){return setStatus(error.message,'bad')}mode=next;document.querySelectorAll('[data-mode]').forEach(button=>button.classList.toggle('active',button.dataset.mode===mode));byId('graph').hidden=mode!=='graph';byId('json').hidden=mode!=='json';document.querySelector('.tools').style.display=mode==='graph'?'flex':'none';mode==='graph'?renderGraph():byId('json').value=JSON.stringify(recipe.definition,null,2)}async function request(path,options={}){const response=await fetch(path,{...options,headers:{'Content-Type':'application/json'}}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed: '+response.status);return data}async function validate(){const button=byId('validate');button.disabled=true;try{const result=await request('/api/validate',{method:'POST',body:JSON.stringify({definition:definitionFromEditor()})});recipe.definition=result.definition;mode==='graph'?renderGraph():byId('json').value=JSON.stringify(recipe.definition,null,2);setStatus('Definition is valid','good')}catch(error){setStatus(error.message,'bad')}finally{button.disabled=false}}async function save(){const button=byId('save');button.disabled=true;try{recipe.definition=definitionFromEditor();const result=await request('/api/recipe',{method:'PUT',body:JSON.stringify(recipe)});recipe=result.recipe;dirty=false;byId('page-title').classList.remove('dirty');renderHeader();renderGraph();setStatus('Saved','good')}catch(error){setStatus(error.message,'bad')}finally{button.disabled=false}}
metadata();renderHeader();bindField('name',()=>recipe.name,value=>{recipe.name=value;byId('page-title').textContent=value});bindField('category',()=>recipe.category||'',value=>recipe.category=value);bindField('description',()=>recipe.description||'',value=>recipe.description=value);bindField('functions',()=>metadata().applicableFunctions.join('\\n'),value=>metadata().applicableFunctions=value.split(/\\n/).map(v=>v.trim()).filter(Boolean));bindField('solution',()=>metadata().solution,value=>metadata().solution=value);renderCollection('requiredInputs');renderCollection('expectedOutputs');renderGraph();document.querySelectorAll('[data-add]').forEach(button=>button.onclick=()=>{const name=button.dataset.add;metadata()[name].push({name:'',description:'',...(name==='requiredInputs'?{required:true}:{})});renderCollection(name);markDirty()});document.querySelectorAll('[data-mode]').forEach(button=>button.onclick=()=>setMode(button.dataset.mode));byId('validate').onclick=validate;byId('save').onclick=save;byId('add-node').onclick=addNode;byId('zoom-out').onclick=()=>{zoom=Math.max(.6,zoom-.1);byId('zoom-reset').textContent=Math.round(zoom*100)+'%';renderGraph()};byId('zoom-in').onclick=()=>{zoom=Math.min(1.5,zoom+.1);byId('zoom-reset').textContent=Math.round(zoom*100)+'%';renderGraph()};byId('zoom-reset').onclick=()=>{zoom=1;byId('zoom-reset').textContent='100%';renderGraph()};byId('json').oninput=markDirty;window.onkeydown=event=>{if(event.key==='Escape'&&selectedNodeId)closeEditor()};window.onbeforeunload=event=>{if(dirty){event.preventDefault();event.returnValue=''}};
const recipeEnvironments=${safeJson(environments)};
const baseRecipeRenderEditor=renderEditor,baseRecipeCommitEditor=commitEditor;
function recipeScriptNumber(id,fallback,min,max){const value=Number.parseInt(byId(id)?.value||'',10);return Math.min(max,Math.max(min,Number.isFinite(value)?value:fallback))}
renderEditor=function(){
  baseRecipeRenderEditor();
  const node=currentNode();if(!node)return;
  const kindInput=byId('node-kind');
  if(kindInput){
    const kindSelect=document.createElement('select');kindSelect.id='node-kind';
    const kinds=[['pkm.step.noop/v1','Step'],['pkm.step.command/v1','Background command'],['pkm.step.script/v1','Executable script'],['pkm.gate.human/v1','Required user input']];
    if(!kinds.some(([kind])=>kind===node.kind))kinds.push([node.kind,node.kind]);
    kindSelect.innerHTML=kinds.map(([kind,label])=>'<option value="'+esc(kind)+'" '+(kind===node.kind?'selected':'')+'>'+esc(label)+'</option>').join('');
    kindInput.replaceWith(kindSelect);
    kindSelect.onchange=()=>{const previous=node.kind;node.kind=kindSelect.value;if(node.kind==='pkm.step.script/v1'&&previous!==node.kind)node.config={runtime:'bash',script:'set -e\\n',timeoutSeconds:300,maxOutputBytes:65536};markDirty();renderEditor()};
  }
  if(node.kind!=='pkm.step.script/v1')return;
  node.config||={runtime:'bash',script:'',timeoutSeconds:300,maxOutputBytes:65536};
  if(editorTab==='intent'){
    const grid=byId('node-instruction')?.closest('.editor-grid');if(!grid)return;
    const runtime=node.config.runtime||'bash',options=recipeEnvironments.map(environment=>'<option value="'+esc(environment.id)+'" '+(environment.id===node.config.environmentId?'selected':'')+'>'+esc(environment.name)+' · '+esc(environment.python||'missing interpreter')+'</option>').join('');
    grid.insertAdjacentHTML('beforeend','<label>Script runtime</label><select id="script-runtime"><option value="bash" '+(runtime==='bash'?'selected':'')+'>Bash · Linux / macOS</option><option value="powershell" '+(runtime==='powershell'?'selected':'')+'>PowerShell · Windows</option><option value="python" '+(runtime==='python'?'selected':'')+'>Python · PKM Environment</option></select>'+(runtime==='python'?'<label>PKM Python Environment</label><select id="script-environment" required><option value="">Select an Environment…</option>'+options+'</select>':'')+'<label class="wide">Script</label><textarea class="wide" id="script-source" rows="12" spellcheck="false">'+esc(node.config.script||'')+'</textarea><small class="wide">'+(runtime==='bash'?'Runs only on Linux and macOS.':runtime==='powershell'?'Runs only on Windows.':'Uses the selected Environment interpreter exactly; no PATH fallback.')+'</small>');
    byId('script-runtime').onchange=()=>{node.config.runtime=byId('script-runtime').value;if(node.config.runtime==='python')node.config.environmentId||=recipeEnvironments[0]?.id||'';else delete node.config.environmentId;markDirty();renderEditor()};
  }else if(editorTab==='parameters'){
    const grid=byId('node-mode')?.closest('.editor-grid');if(!grid)return;
    grid.insertAdjacentHTML('beforeend','<strong class="wide">Script limits</strong><label>Working directory</label><input id="script-cwd" value="'+esc(node.config.cwd||'')+'" placeholder="\${inputs.workspace}"><label>Timeout seconds</label><input id="script-timeout" type="number" min="1" max="3600" value="'+esc(node.config.timeoutSeconds||300)+'"><label>Output limit bytes</label><input id="script-output-limit" type="number" min="1024" max="1048576" value="'+esc(node.config.maxOutputBytes||65536)+'">');
  }
};
commitEditor=function(){
  const node=currentNode(),runtime=byId('script-runtime')?.value,source=byId('script-source')?.value,environmentId=byId('script-environment')?.value,cwd=byId('script-cwd')?.value;
  baseRecipeCommitEditor();
  if(!node||node.kind!=='pkm.step.script/v1')return;
  node.config||={};if(runtime)node.config.runtime=runtime;if(source!==undefined)node.config.script=source;
  if(node.config.runtime==='python'){if(environmentId!==undefined)node.config.environmentId=environmentId}else delete node.config.environmentId;
  if(cwd!==undefined){if(cwd.trim())node.config.cwd=cwd.trim();else delete node.config.cwd}
  if(byId('script-timeout'))node.config.timeoutSeconds=recipeScriptNumber('script-timeout',300,1,3600);
  if(byId('script-output-limit'))node.config.maxOutputBytes=recipeScriptNumber('script-output-limit',65536,1024,1048576);
};
</script></body></html>`;
}